/**
 * User Module. Owns user identity, preference and settings. Depends on
 * Preference. Also owns the shared Tag entity (ADR-0010). Token issuance for
 * the whole system is centralized here (ARCHITECTURE §17).
 */
package com.sololifeos.user;
