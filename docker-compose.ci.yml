# CI 环境覆盖配置：使用 ephemeral 容器，不持久化数据卷。
# Usage: docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d
services:
  postgres:
    volumes: []
    tmpfs:
      - /var/lib/postgresql/data
  redis:
    volumes: []
    tmpfs:
      - /data
